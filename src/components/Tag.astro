---
const { active, activatable = true } = Astro.props;
---
<div class:list={[active ? "bg-secondary text-onSecondary" : "bg-surface-high text-onSurface", activatable && "hover:brightness-125 transition-[filter]", "px-4 py-1 rounded-[4px]"]}>
  <slot />
</div>