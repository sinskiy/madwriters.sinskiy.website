---
const { active } = Astro.props;
---

<div
  class:list={[
    active ? "bg-secondary text-onSecondary" : "bg-surface text-onSurface",
    !active && "transition-colors hover:bg-surface-high",
    "rounded-[4px] px-4 py-1",
  ]}
>
  <slot />
</div>
